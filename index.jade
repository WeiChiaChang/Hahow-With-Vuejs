h1 使用 jQuery  AJAX 與 Vue.js 配合
hr
#app1
  h2 1.使用 Ajax 載入純文字
  h3 {{notifyData}}
hr
#app2
  h2 2.使用Ajax載入商品清單(JSON Array)
  //- 藉由判斷items的長度可以在資料未載入完全時顯示載入中！
  h4(v-if="items.length==0") 載入中．．．．
  ul.items
    li(v-for="item in items") 
      .name {{item.name}}
      .price ${{item.price}}
hr
#app3
  h2 3.實戰載入 Hahow API 資料
  h4(v-if="classes.length==0") 載入中．．．．
  ul.classes
    li(v-for="class in classes") 
      img.classimg(src="{{class.coverImage.url}}")
      .classinfo
        h4 {{class.title}} (${{class.price}})
        h5 老師：{{class.owner.name}}
        h5 募資狀態：{{class.numSoldTickets}} / {{class.successCriteria.numSoldTickets}}
        p {{class.metaDescription}}